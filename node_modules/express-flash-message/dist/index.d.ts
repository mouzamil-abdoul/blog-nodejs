import { Request, Response, NextFunction } from 'express';
interface FlashOption {
    sessionKeyName?: string;
}
declare global {
    namespace Express {
        interface Request {
            flash: (event: string, message: string) => Promise<void>;
            consumeFlash: (event: string) => Promise<string[]>;
        }
    }
}
export declare const flash: (options?: FlashOption | undefined) => (req: Request<import("express-serve-static-core").ParamsDictionary, any, any, import("express-serve-static-core").Query>, response: Response<any>, next: NextFunction) => void;
export {};
